html
    head(lang="pl")
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width;initial-scale=1.0;shrink-to-fit=no")
    body
    
        form(action="/send-data" method="POST" enctype="multiparty/form-data")
            
            input(name="file " type="file" multiple="multiple" accept="image/*")
            
            input(name="user" type="text" required='required')
            
            input(type="submit" value="send a files")
            
        main
            
        
        script(async src="script.js")